-- Benchmark más ligero para el intérprete
-- Fibonacci de 25 (alcanzable sin esperar minutos)

funcion fib(n: float): float {
    si n <= 1.0 {
        dar n
    }
    dar fib(n - 1.0) + fib(n - 2.0)
}

resultado: float = fib(25.0)
decir(resultado)
